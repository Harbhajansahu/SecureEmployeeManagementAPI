spring.application.name=SpringBoot-SecureEmployeeManagementAPI

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/security_project}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:harry@8828}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

jwt.secret=${JWT_SECRET:my-super-secret-key-for-security-project-123456}

server.port=${PORT:8080}
